import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    navbarToolbar: {
        background: '#7aab7f',
    },
    navbarButton: {
        textDecoration: 'none',
        fontWeight: 'bold',
        color: '#ffffff',
        textShadow: "#000000",
    },
  }));